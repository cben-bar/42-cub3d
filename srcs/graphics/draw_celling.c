#include "../../includes/cube.h"
/*
void	ft_draw_ceiling(t_data *data, int color)
{
	int	x;
	int	y;

	x = 0;
	while (x < WIN_W)
	{
		y = 0;
		while (y < WIN_H / 2)
		{
			my_mlx_pixel_put(data->map_img->addr, x, y, color);
			y++;
		}
		x++;
	}
}

*/